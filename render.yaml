services:
  - type: web
    name: ai-automation-blueprint
    env: docker
    dockerfilePath: ./Dockerfile
    region: frankfurt
    plan: free
    autoDeploy: true
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
