name: Run KPI Dashboard

on:
  schedule:
    # Runs daily at 09:00 UTC
    - cron: '0 9 * * *'
  workflow_dispatch: # Allows manual triggering

jobs:
  run_kpi_dashboard:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x' # Use a compatible Python version

      - name: Install Python dependencies
        run: pip install -r LinktoFunnel/requirements.txt

      - name: Run KPI Dashboard Script
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python LinktoFunnel/kpi_monitoring_dashboard.py

      # Optional: Add steps here to process or store the output, e.g., commit current_dashboard.json
      # - name: Commit Dashboard Snapshot
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "github-actions[bot]@users.noreply.github.com"
      #     git add current_dashboard.json
      #     git commit -m "feat: Update KPI dashboard snapshot" || echo "No changes to commit"
      #     git push
